# 查看磁盘命令

df -h 查看文件系统的磁盘空间使用情况

du -sh /root 显示文件或目录的磁盘使用情况
- -s, --summarize 仅显示总计，只列出最后加总的值。
lsblk 列出系统中的所有块设备（磁盘，分区，挂载点）

### 新建磁盘操作

第一步，在vmware上，选择我的计算机，自己新建的主机，右键单机，选择电源-关闭客户机
第二步，在vmware上，选择我的计算机，自己新建的主机，右键单机，选择最底下的设置
第三步，在虚拟机设置点击添加
第四步，添加硬盘，点下一步，选择scsi，点下一步，选创建新虚拟磁盘，点下一步，磁盘大小随意，点下一步，点完成，点确定

### 磁盘分区

创建新分区 fdisk （以 /dev/sdb 为例）
fdisk /dev/sdb
输入 n 来创建新分区。
选择分区类型（主分区 p 或扩展分区 e）。
设置分区号1、起始位置和结束位置（默认即可）。
输入 w 保存分区表并退出。
此时就创建了一个## sdb1的第一个分区 `/dev/sdb1` 

###  `/dev/sdb` 和 `/dev/sdb1` 的本质关系

可以这样理解：

| 名称          | 含义                 |
| ----------- | ------------------ |
| `/dev/sdb`  | 整块磁盘               |
| `/dev/sdb1` | 这块磁盘上的第 1 个分区      |
| `/dev/sdb2` | 这块磁盘上的第 2 个分区      |
| `/dev/sdb5` | 有时是扩展分区里的第 1 个逻辑分区 |

## 挂载时挂的是谁

挂载挂的是“有文件系统的设备”。

通常流程：

bash

fdisk /dev/sdb          # 对整盘分区
mkfs.ext4 /dev/sdb1     # 对第 1 个分区格式化
mkdir /data
mount /dev/sdb1 /data   # 把第 1 个分区挂到 /data

挂载后：

text

访问 /data
   ↓
实际访问 /dev/sdb1
   ↓
也就是 /dev/sdb 这块盘的第 1 个分区

`/dev/sdb` 仍然存在，但你不直接挂它，因为文件系统建在 `/dev/sdb1` 上。

创建新分区后，需要将其格式化为文件系统。例如，使用 ext4 文件系统。
格式化为 ext4 文件系统：
mkfs.ext4 /dev/sdb1

也可以格式化为xfs 文件系统
mkfs.xfs /dev/sdb1

创建挂载点（如 /data）
mkdir /data
挂载分区到
mount /dev/nvme0n2p1 /data

设置系统自启动挂载
查看/etc/fstab
vim /etc/fstab 


/dev/nvme0n2p1 /mnt/data ext4      default 0 0 
设置挂载分区  设置挂载点 设置文件系统格式 

mount -a
-a：将 /etc/fstab 中定义的所有档案系统挂上

![[Pasted image 20260922235302.png]]


保存时不要使用大写X这个是加密
如果使用加密再重新加载会导致系统识别不到配置文件 
如果加密了进入该文件
vim /etc/fstab 输入密码
设置
:set key=
:wq

重新加载看看
mount -a
